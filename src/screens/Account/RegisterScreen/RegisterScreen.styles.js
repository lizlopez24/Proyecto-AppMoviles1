import { StyleSheet } from "react-native";

export const styles=StyleSheet.create({
    image:{
        resizeMode:"contain",
        height:200,
        width:"100%",
        marginTop:30,
    },
    content:{
        marginHorizontal:40
    }
});